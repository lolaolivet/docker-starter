{% extends 'base.html.twig' %}

{% block title %}Hello {{line.name}} 🇨🇭!{% endblock %}

{% block body %}

<div class="example-wrapper">
    <a class="link" href="{{path('lines')}}"><h1>Hello {{line.name}} 🇨🇭!</h1></a>

    <div class="difficultyRow">
        {% for d in line.difficulty %}
            <div class="difficulty">
                <p style="color: {{d.colour}}; font-size: 14px;">{{d.difficulty}}</p>
            </div>
        {% endfor %}
    </div>

    <div class="feedbackContainer">

        {% for feedback in line.feedbacks %}
            <div class="feedbackRow">
                <div style="width: 25%">
                    <p>{{ feedback.date|date('d F Y') }}</p>
                </div>
                <div style="width: 5%">
                    <p>{{ feedback.rate }}</p>
                </div>
                <div style="width: 70%">
                    <p>{{ feedback.comment }}</p>
                </div>
            </div>
        {% endfor %}
    </div>

    {{form(form)}}

<div>
    
    
    
</div>
{% endblock %}
